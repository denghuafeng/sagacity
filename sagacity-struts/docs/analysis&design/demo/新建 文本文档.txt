对半分图

上面tab还是要的

还有就是图标要改一下?

当对半分时两个tab分列在之前的tab区域

然后区域的之前的两个图标???

一个是关闭还有对半分的图标适当改变一下

对半分的图标改成合并成tab

还有就是每个操作选项要适当改变一下,目的是，当正常点击车间控制时就在工作区域关闭所有打开的页面，单独打开新的页面
如果点后面的图标就是表明是在现有的tab情况下追加一个tab
