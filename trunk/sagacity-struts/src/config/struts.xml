<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>
	<constant name="struts.i18n.reload" value="true" />
	<constant name="struts.objectFactory" value="spring" />
	<constant name="struts.devMode" value="true" />
	<constant name="struts.custom.i18n.resources"
		value="globalMessages" />
	<package name="default" extends="struts-default">
		<interceptors>
			<interceptor-stack name="sagacityStack">
				<interceptor-ref name="checkbox">
					<param name="uncheckedValue">0</param>
				</interceptor-ref>
				<interceptor-ref name="defaultStack" />
			</interceptor-stack>
		</interceptors>
		<default-interceptor-ref name="sagacityStack" />
		<!-- 定义全局处理结果 -->
		<global-results>
			<!-- 逻辑名为sql的结果，映射到/exception.jsp页面 -->
			<result name="globeException">/common/error.jsp</result>
		</global-results>
		<global-exception-mappings>
			<!-- 当Action抛出SQLException异常时，转入名为sql的结果 -->
			<exception-mapping exception="org.sagacity.framework.exception.BaseException" 
				result="globeException" />
		</global-exception-mappings>
	</package>
	<include file="struts-project.xml" />
</struts>
